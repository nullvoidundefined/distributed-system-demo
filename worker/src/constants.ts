/** Worker defaults and simulation constants; env vars override the DEFAULT_* values at runtime. */

export const DEFAULT_HEARTBEAT_INTERVAL_MS = 2000;
export const DEFAULT_LOCK_DURATION_MS = 4000;
export const DEFAULT_MAX_STALLED_COUNT = 10;
export const DEFAULT_STAGE_MS = 2500;
export const DEFAULT_STALLED_INTERVAL_MS = 2000;
export const RENDER_STEPS = 5;
